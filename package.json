{
  "scripts": {
    "codegen": "graph codegen",
    "build": "rm -rf build & graph build",
    "gcreate": "graph create",
    "gremove": "graph remove",
    "gdeploy": "graph deploy"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.61.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "@openzeppelin/subgraphs": "^0.1.8-5"
  }
}
